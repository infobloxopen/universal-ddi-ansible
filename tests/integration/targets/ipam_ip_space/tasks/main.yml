---
- module_defaults:
    group/infoblox.universal_ddi.all:
      portal_url: "{{ portal_url }}"
      portal_key: "{{ portal_key }}"
  block:
    # Basic tests for IP space
    - name: fetch all IP Space objects
      ansible.builtin.set_fact:
        ip_space: "{{ lookup('infoblox.universal_ddi.universal_ddi_lookup','ipam/ip_space', fields=['id', 'name', 'comment'] , provider={ 'portal_url':'https://stage.csp.infoblox.com' , 'portal_key': '14d630e1ffebd17835bfc5640389705ee983c130436c9acd35d4e2524c39c719'}) }}"
